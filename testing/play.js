const assert = require('assert');

const firstArray = [1, 2, 3]
const secondArray = firstArray

// assert.equal() checks the object is the same
// assert.strictEqual() checks object and type is equals
// assert.deepEqual() checks the attributes of the object are same
// assert.deepStrictEqual() checks the attributes of the object and the type are the same